{
  "name": "autonomous-ai-swarm",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace web",
    "build": "npm run build --workspace web",
    "lint": "npm run lint --workspace web",
    "typecheck": "npm run typecheck --workspace web",
    "format": "npm run format --workspace web"
  },
  "engines": {
    "node": ">=20.11.0"
  }
}
